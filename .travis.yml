language: objective-c

before_install:
  - cd Example

install:
  - gem install cocoapods --no-ri --no-rdoc
  - gem install xcpretty --no-ri --no-rdoc
  - pod install

script:
  - xcodebuild -workspace Example.xcworkspace -scheme OpinionatedC test | xcpretty -c
